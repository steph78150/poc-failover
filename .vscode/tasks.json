{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/poc-failover/poc-failover.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}